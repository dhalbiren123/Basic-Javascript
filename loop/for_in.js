//for in loop
const abc={
    name:"bbsr",
    pin:751003,
    dist:'khordha'
}
for(let r in abc)
{
    console.log(r,abc[r]);

    //console.log(`${r}: ${abc[r]}`)//${key}:${objectname[value]}
    
}

